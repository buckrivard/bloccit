<h1>New Question</h1>

<div class="row">
    <div class="col-md-4">
      <p>Guidelines for quesetions</p>
      <ul>
        <li>No rhymes.</li>
        <li>Avoid alliteration always.</li>
        <li>There are such things as stupid questions.</li>
      </ul>
    </div>
    <div class="col-md-8">
   		<%= form_for @question do |f| %>
   		<div class="form-group">
   			<%= f.label :title %>
			<%= f.text_field :title, class: 'form-control', placeholder: "Enter question header" %>
   		</div>
   		<div class="form-group">
   			<%= f.label :body %>
			<%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter post body" %>
   		</div>
   		<div class="form-group">
   		<%= f.submit "Save", class: 'btn btn-success' %>
   		</div>
   		<% end %>
    </div>
</div>



